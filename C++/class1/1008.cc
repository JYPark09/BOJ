#include <bits/stdc++.h>

int main()
{
    std::ios::sync_with_stdio(false);

    long double a, b;
    std::cin >> a >> b;

    std::cout << std::fixed << std::setprecision(9) << a / b << std::endl;
}
